# API Configuration
# For local development (HTTP)
# VITE_API_URL=http://localhost:2000/api/v1
# For production (HTTPS) - the protocol will be automatically matched to the current page
# VITE_API_URL=https://takemyphoto.xyz/api/v1
# Leave empty to use relative URLs (recommended - uses current page protocol)
VITE_API_URL=
